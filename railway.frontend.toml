# Railway configuration for JBS Internal Portal Frontend
# Astro frontend only

[build]
builder = "nixpacks"
buildCommand = "cd frontend && npm ci && npm run build"

[deploy]
startCommand = "cd frontend && npm run start"
# Remove healthcheck - let Railway auto-detect readiness
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
