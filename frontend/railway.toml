[build]
builder = "NIXPACKS"
buildCommand = "rm -rf node_modules/.vite && rm -rf dist && npm install --legacy-peer-deps && npm run build"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[nixpacks]
nodeVersion = "20"
